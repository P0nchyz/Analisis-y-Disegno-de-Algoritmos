# Compiler
CC = gcc

# Compiler flags
CFLAGS = -Wall

# Library files
LIB = lib/tiempo.c lib/searching/searching.c lib/searching/thread_searching.c

# Output executable
TARGET = search.out

all: main.c $(LIB)
	$(CC) -o $(TARGET) main.c $(LIB)
